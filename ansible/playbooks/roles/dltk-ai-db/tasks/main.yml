---
# tasks file for dltk-ai-db.yml

- name: Create dltk-ai directory if it does not exist
  ansible.builtin.file:
    path: "{{ folderpath }}/dltk-ai"
    state: directory
    mode: '0755'

- name: Create dltk-ai directory if it does not exist
  ansible.builtin.file:
    path: /usr/dltk-ai
    state: directory
    mode: '0755'

- name: copying the required folder from ansible to remote machine
  copy:
    src: "{{ playbook_dir }}/../../db"
    dest: "{{ folderpath }}/dltk-ai/"

- name: running docker-compose.yml file to stop dltk-influxdb and dltk-redis containers 
  command: chdir="{{ folderpath }}/dltk-ai/db/" docker-compose stop dltk-redis dltk-influxdb

- name: Pause for 8 sec
  pause:
    seconds: 8

- name: running docker-compose.yml file to start dltk-influxdb and dltk-redis containers | Setting up OpenDltk services, this may take some time.
  command: chdir="{{ folderpath }}/dltk-ai/db/" docker-compose up -d dltk-redis dltk-influxdb
