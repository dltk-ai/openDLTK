---
# tasks file for dltk-ai-uninstall-cv-image-classification
- name: Create a directory if it does not exist
  ansible.builtin.file:
    path: "{{ folderpath }}/dltk-ai"
    state: directory
    mode: '0755'

- name: run docker-compose.yml file
  command: chdir="{{ folderpath }}/dltk-ai/pretrained_detectors/" docker-compose down
